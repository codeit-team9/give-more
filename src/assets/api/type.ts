type UserData = {
  token: string;
  user: string[];
  id: string;
  email: string;
  type: string;
  href: string;
  links: string[];
};

type Test = {
  id: string;
};

export type { UserData, Test };
